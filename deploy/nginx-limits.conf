limit_req_zone $binary_remote_addr zone=landing_api:10m rate=15r/s;
limit_req_zone $binary_remote_addr zone=landing_login:10m rate=3r/m;
limit_req_zone $binary_remote_addr zone=landing_leads:10m rate=2r/m;
limit_conn_zone $binary_remote_addr zone=landing_conn:10m;

map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
